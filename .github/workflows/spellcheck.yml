name: Spellcheck

on: push

jobs:
  spellcheck:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - run: |
          pip install codespell    
          codespell -S deps
